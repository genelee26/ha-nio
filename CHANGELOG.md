# 更新日志 / Changelog

本项目（HACS 自定义集成 `nio`）的版本更新记录，新版在上。

## v0.4.4 — 2026-06-19

诊断增强：精确脱敏 + 抓包字段完整性。

**改进（诊断）**
- 诊断包里的 status `query` 由**整条打码**改为**精确脱敏**：只盖 `sign` / `device_id` / `timestamp`，**保留 `field=` 列表与 `app_ver`**。部分传感器长期「未知」多半是抓包的 `field=` 不全（签名锁死、补不了字段），现在分享出来的诊断包能直接看出来。
- 诊断包新增 `capture_fields`：`requested`（抓包请求了哪些段）+ `missing_critical`（缺了哪些关键段：exterior / soc / door / window / hvac / fota）—— 一眼区分「抓包字段不全 / 车离线 / 字段名不同」三种成因。
- 调试子页说明追加：下载诊断后建议到 `github.com/genelee26/ha-nio/issues` 提 issue 并附文件，方便跟踪。

**新增**
- 抓包字段完整性检查：启动时若捕获请求的 `field=` 缺关键段（exterior / soc / door / window / hvac / fota），在「设置 → 维修」弹**非阻塞警告**，引导从车辆主页下拉刷新重抓完整请求；抓包补全后自动消除。

## v0.4.3 — 2026-06-18

车卡布局整合 + 弹窗原生化 + 账单筛选。

**改进（车辆卡片）**
- 底栏图标 **6 → 3**，窄屏 / 移动端不再被挤掉：车辆状态 / 安防 / 账单。电量移到车图**右上角徽章**（充电感知，<20% 变红）。
- 车辆状态合并为单字形（行驶 / 驻车 / 睡眠），以 `车辆状态` 传感器为准；安防合并门 / 窗 / 锁，异常时点名 `门·窗·锁`；账单图标换成 `mdi:order-bool-descending-variant`。
- **逐图标下钻**：电量 → 该实体的原生 more-info（历史曲线）；车辆状态 / 安防 → 各自聚焦弹窗，只列其成员实体；点车身 → 全量总览。

**改进（弹窗）**
- 状态 / 安防 / 账单三个弹窗统一成 **HA 原生 more-info 风格**：✕ 在左、设备面包屑 + 大标题、右上操作图标，行用 `ha-state-icon`（按状态着色）+ `ha-relative-time`；点任一行进入该实体的真原生 more-info。
- 账单弹窗新增**订单类型筛选**（右上下拉，8 种逐项带数量，多选 + 一键全选 / 全不选）：作用于概览 / 月份小计 / 月份范围 / 明细，筛选生效时图标高亮。

## v0.4.2 — 2026-06-17

调试能力 + 账单弹窗打磨。

**新增**
- 配置项「**调试模式**」开关（设置 → 设备与服务 → NIO → 配置 → 调试）。
- 服务 `nio.inject_debug_order` / `nio.clear_debug_orders`（**仅开发者**）：注入/清除调试账单（内置样例，或自定义原始订单 dict）。调试单标记 `debug`、持久化进 `.storage`；调试**关**时隐藏、不计数、**永不进长期统计**，调试**开**时显示。
- 配置项**诊断下载**（设置 → 集成 → NIO → ⋮ → 下载诊断）：脱敏调试包（token / GPS / VIN 全打码），含最近 status / orders **原始服务器返回** + 账本；下载时自动抓一次最新原始返回，方便发开发者定位问题。调试模式下刷新按钮会提示去下载诊断。

**改进（账单弹窗）**
- 头部改为**按类型计数**（可扩展到全部 8 种服务）+ 累计花费；中国时区**月份导航** + **单订单翻页**。
- 支持「**灵活升级**」订单形态（取/还双站点 + `extendInfo` 费用）；状态徽章按蔚来原文着色。
- 行距加大、毛玻璃背景、底部「订单刷新」。
- 弹窗**顶部锚定**：切月份时框高变化不再让箭头跳动；空月保持尺寸、居中显示「本月无订单」。

## v0.4.1 — 2026-06-17

**新增**：车卡底栏「账单」图标 → 账单/订单弹窗（三段：总览 / 月导航 / 单订单明细）。数据走响应式服务 `nio.get_service_orders`，按需拉取、不进 state machine / recorder。

## v0.4.0 — 2026-06-17

**新增**：服务订单 / 换电账单追踪。10 个传感器（累计 / 最近 / 本月换电花费与次数、维保、同步状态）+ 手动刷新按钮 + **长期统计**（按每笔订单真实下单时间回填，曲线可追溯到首单）。独立 12h 慢 coordinator + 节流可续回填，与车况轮询分离，订单网关异常不拖垮核心集成。

## v0.3.1 — 2026-06-16

**修复**：配置表单的 `Translation error: UNCLOSED_TAG`（错误文案里的 `<id>` 被 HA 前端 ICU 当成未闭合标签）；`parse_capture` 容忍整行抓包粘贴（前缀 `GET …`、后缀 ` HTTP/2`、后跟请求头）。

## v0.3.0 — 2026-06-16

**修复**：凭证改为「整条 status 请求 URL **原样重放**」。此前按字段重建请求、硬编码 `app_ver` 与字段列表，蔚来 App 升级（如 6.6.0 新增 `field=key`）后签名失效，且被**误报成「token 被拒」**。现逐字节重放抓包，签名三轴自动对齐；旧版**无感迁移**（v1→v2），现有装机无需重新抓包。错误码区分「签名失败 / token 失败」。

## v0.2.x 及更早

自定义集成成型期：从散装 YAML 重构为 HACS 自定义集成（domain `nio`）；自带 `nio-car-card` Lovelace 卡片（官方车图 + 状态图标 + 详情弹窗 + 可视化编辑器）；逐字段 config flow；本地品牌图标；GCJ-02→WGS-84 坐标转换；门窗/锁语义真车实测修正；自适应轮询 cadence。
