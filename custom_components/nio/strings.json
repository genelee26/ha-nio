{
  "config": {
    "step": {
      "user": {
        "title": "Connect your NIO vehicle",
        "description": "Sniff one vehicle-status request from the NIO app (MITM proxy: icar.nio.com/api/2/rvs/vehicle/.../status) and paste the whole request URL below, plus its Authorization Bearer token. The URL is replayed exactly as captured — don't edit it; the server's signature covers the entire query.",
        "data": {
          "capture": "Captured status request URL (the whole .../status?... line)",
          "token": "Bearer token (Authorization header)",
          "model": "Vehicle model"
        }
      },
      "reauth_confirm": {
        "title": "Re-authenticate NIO",
        "description": "Paste a fresh Bearer token. If the signature was rejected (e.g. the NIO app updated), also paste a newly captured status request URL to refresh it.",
        "data": {
          "token": "Bearer token (Authorization header)",
          "capture": "Captured status request URL (optional — only if re-sniffed)"
        }
      }
    },
    "error": {
      "invalid_auth": "The NIO API rejected the token. Re-sniff a fresh Bearer token.",
      "invalid_sign": "The NIO API rejected the signature, not the token — the captured request no longer matches (the app likely updated). Re-sniff a current status request and paste the whole URL.",
      "invalid_url": "Couldn't parse that as a status request. Paste the whole line containing /vehicle/.../status?...&sign=...",
      "cannot_connect": "Could not reach the NIO API."
    },
    "abort": {
      "already_configured": "This vehicle is already configured.",
      "reauth_successful": "Re-authentication was successful."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "NIO options",
        "menu_options": {
          "intervals": "Polling cadence",
          "credentials": "Credentials (token)",
          "debug": "Debug mode"
        }
      },
      "debug": {
        "title": "Debug mode",
        "description": "When on, injected debug orders show and count in the popup/sensors (never in long-term statistics), and the refresh buttons prompt to download diagnostics for the developer. After downloading the diagnostics, please open an issue at github.com/genelee26/ha-nio/issues and attach the file so it can be tracked.",
        "data": { "debug": "Enable debug mode" }
      },
      "intervals": {
        "title": "Polling cadence",
        "description": "Polling the private API too aggressively can get the token rate-limited or invalidated.",
        "data": {
          "interval_driving": "Interval while driving (minutes)",
          "interval_day": "Daytime interval (minutes)",
          "interval_night": "Night interval (minutes)",
          "day_start_hour": "Daytime starts at (hour)",
          "day_end_hour": "Daytime ends at (hour)"
        }
      },
      "credentials": {
        "title": "Update credentials",
        "description": "Paste a fresh Bearer token. To refresh the captured request too (e.g. after a NIO app update), also paste a newly captured status request URL.",
        "data": {
          "token": "Bearer token (Authorization header)",
          "capture": "Captured status request URL (optional — only if re-sniffed)"
        }
      }
    },
    "error": {
      "invalid_auth": "The NIO API rejected the token. Re-sniff a fresh Bearer token.",
      "invalid_sign": "The NIO API rejected the signature, not the token — the captured request no longer matches (the app likely updated). Re-sniff a current status request and paste the whole URL.",
      "invalid_url": "Couldn't parse that as a status request. Paste the whole line containing /vehicle/.../status?...&sign=...",
      "cannot_connect": "Could not reach the NIO API."
    }
  },
  "services": {
    "get_service_orders": {
      "name": "Get service orders",
      "description": "Return the full NIO service-order (battery swap / maintenance / billing) history for a vehicle.",
      "fields": {
        "device_id": {
          "name": "Vehicle",
          "description": "The NIO device to query (optional if only one vehicle is configured)."
        },
        "include_cancelled": {
          "name": "Include cancelled",
          "description": "Include cancelled orders in the result."
        }
      }
    },
    "inject_debug_order": {
      "name": "Inject debug order",
      "description": "Developer-only: inject a debug billing order (a built-in sample or a raw order dict). Flagged debug — hidden unless debug mode is on, and never counted in long-term statistics.",
      "fields": {
        "device_id": { "name": "Vehicle", "description": "The NIO device (optional if only one vehicle is configured)." },
        "sample": { "name": "Built-in sample", "description": "A bundled sample order to inject." },
        "order": { "name": "Raw order", "description": "A raw order dict (as returned by getTabOrder) to inject instead of a sample." }
      }
    },
    "clear_debug_orders": {
      "name": "Clear debug orders",
      "description": "Developer-only: remove all injected debug orders from the ledger.",
      "fields": {
        "device_id": { "name": "Vehicle", "description": "The NIO device (optional if only one vehicle is configured)." }
      }
    }
  },
  "entity": {
    "sensor": {
      "battery": { "name": "Battery" },
      "remaining_range": { "name": "Range (CLTC)" },
      "remaining_actual_range": { "name": "Range (actual)" },
      "range_achievement_rate": { "name": "Range achievement rate" },
      "vehicle_state": {
        "name": "Vehicle state",
        "state": {
          "driving": "Driving",
          "parked": "Parked",
          "resting": "Resting",
          "unknown": "Unknown"
        }
      },
      "charging_power": { "name": "Charging power" },
      "inside_temperature": { "name": "Inside temperature" },
      "outside_temperature": { "name": "Outside temperature" },
      "mileage": { "name": "Mileage" },
      "tyre_pressure_front_left": { "name": "Tyre pressure front left" },
      "tyre_pressure_front_right": { "name": "Tyre pressure front right" },
      "tyre_pressure_rear_left": { "name": "Tyre pressure rear left" },
      "tyre_pressure_rear_right": { "name": "Tyre pressure rear right" },
      "fota_version": { "name": "Firmware version" },
      "swap_total_cost": { "name": "Battery swap total cost" },
      "swap_count": { "name": "Battery swap count" },
      "swap_last_time": { "name": "Last swap time" },
      "swap_last_cost": { "name": "Last swap cost" },
      "swap_last_station": { "name": "Last swap station" },
      "swap_month_count": { "name": "Swaps this month" },
      "swap_month_cost": { "name": "Swap cost this month" },
      "maintenance_count": { "name": "Maintenance count" },
      "maintenance_last_time": { "name": "Last maintenance time" },
      "orders_sync": {
        "name": "Orders sync",
        "state": {
          "idle": "Idle",
          "backfilling": "Backfilling history",
          "up_to_date": "Up to date"
        }
      }
    },
    "binary_sensor": {
      "driving": { "name": "Driving" },
      "sleeping": { "name": "Sleeping" },
      "door": { "name": "Doors" },
      "window": { "name": "Windows" },
      "lock": { "name": "Lock" },
      "charging": { "name": "Charging" },
      "connected": { "name": "Cloud connection" }
    },
    "device_tracker": {
      "location": { "name": "Location" }
    },
    "button": {
      "refresh": { "name": "Refresh data" },
      "refresh_orders": { "name": "Refresh service orders" }
    }
  },
  "issues": {
    "incomplete_capture": {
      "title": "NIO capture is missing data fields",
      "description": "The captured status request doesn't ask for these sections: {fields}. The matching sensors (doors, windows, lock, temperatures, firmware…) will stay \"unknown\" — the signature locks the request, so the missing sections can't be added. Fix: capture a status request from the vehicle's main page (pull-to-refresh) so it asks for the full field set, then update the credentials (NIO → Configure → Credentials) with the new capture."
    }
  }
}
